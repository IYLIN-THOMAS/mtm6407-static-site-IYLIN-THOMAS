module.exports = function(eleventyConfig) {
    return {
      dir: {
        input: "src",      // This tells Eleventy your content lives in the "src" folder
        output: "_site"    // This tells Eleventy to build the final site in a folder named "_site"
      }
    };
  };


  